import styled from 'styled-components';

const ModalWrapper = styled.section`
	inset: 0;
	position: fixed;
	overflow-x: hidden;
	overflow-y: auto;
	height: 100vh;
	width: 100vw;
`;

export default ModalWrapper;
