export enum GenderEnum {
  Male = 'male',
  Female = 'female',
  None = 'none',
}

export enum StateEnum {
  Active = 'active',
  Inactive = 'inactive',
  Archived = 'archived',
}

export enum InvitationEnum {
  Pending = 'pending',
  Accepted = 'accepted',
  Rejected = 'rejected',
}